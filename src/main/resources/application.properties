spring.application.name=secure-monkey

# Database Configuration (H2 for development)
spring.datasource.url=jdbc:h2:mem:securemonkey
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sakhile
spring.datasource.password=password
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
# Use a secure 256-bit key (32 bytes)
app.jwt.secret=secure-monkey-jwt-secret-key-2025-secure-key
app.jwt.expiration=86400000
